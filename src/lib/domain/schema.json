{
    "$schema": "http://json-schema.org/schema",
    "id": "SchematicsVigiaDomain",
    "title": "Vigia de Preço Domain Options Schema",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The name of the decorator.",
        "$default": {
          "$source": "argv",
          "index": 0
        },
        "x-prompt": "What name would you like to use for the decorator?"
      },
      "path": {
        "type": "string",
        "format": "path",
        "description": "The path to create the decorator."
      }
    },
    "required": ["name"]
  }